# signature.axir Reference Notes

Reference files:

- `src/ax/dsp/parser.ts` for flat string signature parsing.
- `src/ax/dsp/sig.ts` for `AxField`, `AxFieldType`, `AxSignature`,
  `AxSignatureBuilder`, fluent fields, optional/internal/cache flags, and
  duplicate-field diagnostics.
- `src/ax/dsp/sig.test.ts` and `src/ax/dsp/sig-fluent*.test.ts` for behavior.
