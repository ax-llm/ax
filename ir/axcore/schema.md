# schema.axir Reference Notes

Reference files:

- `src/ax/dsp/jsonSchema.ts` for Ax field to JSON Schema conversion.
- `src/ax/dsp/standardSchema.ts` for Standard Schema conversion.
- `src/ax/ai/types.ts` for `AxFunctionJSONSchema`.
