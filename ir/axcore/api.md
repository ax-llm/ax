# api.axir Reference Notes

Reference files:

- `src/ax/dsp/template.ts` for `s(...)` and `ax(...)`.
- `src/ax/dsp/sig.ts` for `f(...)`, `fn(...)`, `AxSignature`, and tool builders.
- `src/ax/dsp/generate.ts` for `AxGen`.
