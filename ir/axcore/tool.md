# tool.axir Reference Notes

Reference files:

- `src/ax/dsp/functions.ts` for tool call/result processing.
- `src/ax/dsp/sig.ts` for `fn(...)`, argument schemas, return schemas, and
  examples.
- `src/ax/dsp/functions.test.ts` for tool execution edge cases.
