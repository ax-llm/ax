# agent.axir Reference Notes

Reference files:

- `src/ax/agent/*` for future agent runtime extraction.

V1 reserves the public namespace only. It must not generate an executable
`AxAgent` runtime until the AxGen/tool/provider layers are stable.
