# validate.axir Reference Notes

Reference files:

- `src/ax/dsp/extract/structuredJson.ts` for structured output validation.
- `src/ax/dsp/validators.ts` for string, number, URL, and regex constraints.
- `src/ax/dsp/generateValidation.test.ts` for retry and validation behavior.
