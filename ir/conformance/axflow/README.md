# AxFlow Conformance

Fixtures in this suite prove AxFlow as an Ax program graph: graph planning,
barriers, child-program execution, control-flow blocks, explicit parallel
merge behavior, `.returns()` projection, cache/streaming short-circuiting,
stop/abort boundaries, and node demo validation.
