{
  "expected_schema": {
    "additionalProperties": false,
    "properties": {
      "age": {
        "description": "Minimum value: 18, maximum value: 120",
        "maximum": 120,
        "minimum": 18,
        "type": "number"
      },
      "email": {
        "description": "Must be a valid email address format",
        "format": "email",
        "type": "string"
      },
      "outputCode": {
        "description": "Must contain only uppercase letters and numbers",
        "pattern": "^[A-Z0-9]+$",
        "type": "string"
      },
      "username": {
        "description": "Minimum length: 3 characters, maximum length: 20 characters",
        "maxLength": 20,
        "minLength": 3,
        "type": "string"
      },
      "website": {
        "description": "Must be a valid URL format",
        "format": "uri",
        "type": "string"
      }
    },
    "required": ["username", "email", "website", "outputCode", "age"],
    "title": "Schema",
    "type": "object"
  },
  "kind": "json_schema",
  "name": "constraints-and-formats",
  "signature_spec": {
    "inputs": {
      "query": {
        "type": "string"
      }
    },
    "outputs": {
      "username": {
        "max": 20,
        "min": 3,
        "type": "string"
      },
      "email": {
        "email": true,
        "type": "string"
      },
      "website": {
        "type": "string",
        "url": true
      },
      "outputCode": {
        "pattern": "^[A-Z0-9]+$",
        "patternDescription": "Must contain only uppercase letters and numbers",
        "type": "string"
      },
      "age": {
        "max": 120,
        "min": 18,
        "type": "number"
      }
    }
  },
  "target": "outputs"
}
