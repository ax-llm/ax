{
  "expected_schema": {
    "additionalProperties": false,
    "properties": {
      "looseObject": {
        "description": "Loose object. Return this field as a JSON-encoded string that can be parsed with JSON.parse.",
        "type": "string"
      },
      "payloadData": {
        "description": "Flexible payload. Return this field as a JSON-encoded string that can be parsed with JSON.parse.",
        "type": "string"
      }
    },
    "required": ["payloadData", "looseObject"],
    "title": "Schema",
    "type": "object"
  },
  "kind": "json_schema",
  "name": "flexible-json-as-string",
  "schema_options": {
    "flexibleJsonFieldsAsString": true
  },
  "signature_spec": {
    "inputs": {
      "query": {
        "type": "string"
      }
    },
    "outputs": {
      "payloadData": {
        "description": "Flexible payload",
        "type": "json"
      },
      "looseObject": {
        "description": "Loose object",
        "type": "object"
      }
    }
  },
  "target": "outputs"
}
