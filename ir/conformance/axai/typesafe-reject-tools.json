{
  "expected_error_contains": "does not support",
  "expected_transport_request_count": 0,
  "kind": "ai_chat",
  "name": "typesafe-reject-tools",
  "provider": "typesafe",
  "request": {
    "chatPrompt": [
      {
        "content": "Checkout is unavailable",
        "role": "user"
      }
    ],
    "functions": [
      {
        "description": "Lookup",
        "name": "lookup",
        "parameters": {
          "properties": {},
          "type": "object"
        }
      }
    ],
    "modelConfig": {
      "stream": false
    },
    "responseFormat": {
      "fieldDescriptions": {
        "team": {
          "description": "Responsible team",
          "valueDescriptions": {
            "billing": "Payment questions",
            "support": "Product help"
          }
        }
      },
      "schema": {
        "name": "decision",
        "schema": {
          "properties": {
            "team": {
              "enum": ["support", "billing"],
              "type": "string"
            },
            "urgent": {
              "type": "boolean"
            }
          },
          "required": ["urgent", "team"],
          "type": "object"
        }
      },
      "type": "json_schema"
    }
  }
}
