# Go Examples

Go examples are reserved for the future generated Go backend.

The AxIR compiler is written in Go, but the generated Ax libraries currently target Python, Java, and C++. The planned Go package name is `axllm`, with module path `github.com/ax-llm/ax/go`.
